<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".presentation.note.NotesFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/notesRecycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/note_item"/>
    </ScrollView>

    <ImageButton
        android:id="@+id/addNoteButton"
        android:layout_width="@dimen/bigButtonHeightWidth"
        android:layout_height="@dimen/bigButtonHeightWidth"
        android:scaleType="fitCenter"
        android:src="@drawable/add"
        android:layout_marginEnd="@dimen/bigMargin"
        android:layout_marginBottom="@dimen/bigMargin"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@android:color/transparent"
        android:contentDescription="@string/addNote" />

</androidx.constraintlayout.widget.ConstraintLayout>